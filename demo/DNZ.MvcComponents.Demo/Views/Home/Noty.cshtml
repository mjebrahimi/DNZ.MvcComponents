@{
    ViewData["Title"] = "Noty";
}

<h2>Noty</h2>

<table>
    <tr>
        <td>
            <input type="button" value="Click Me" onclick="@Html.Noty("A message from Client (on click)", MessageType.Warning, layout: MessageAlignment.BottomRight).Script" class="btn btn-primary" />
        </td>
        <td>
            @using (Html.BeginForm())
            {
                if (Context.Request.Method.ToLower() == "get")
                {
                    @Html.Noty("A message from Client (on pageload)").Modal(true).Type(MessageType.Information)
                }
                <input type="submit" value="Submit Form" class="btn btn-primary" />
            }
        </td>
        <td>
            @using (Html.AjaxBeginForm(new AjaxOptions()))
            {
                <input type="submit" value="Submit Ajax" class="btn btn-primary" />
            }
        </td>
    </tr>
</table>